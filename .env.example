# Copy this file to .env and fill in your key. .env is git-ignored — never commit it.

# Required: your OpenAI API key (https://platform.openai.com/api-keys)
OPENAI_API_KEY=sk-...

# Optional overrides (defaults shown)
# SCOUTMINI_MODEL=gpt-4o-mini
# SCOUTMINI_SEASON=2024
